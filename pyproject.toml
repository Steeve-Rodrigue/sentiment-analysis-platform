[project]
name = "sentiment-analysis-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "emoji>=2.15.0",
    "nltk>=3.10.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
